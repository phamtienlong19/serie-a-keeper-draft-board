{
  "name": "serie-a-draft-board",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/prepare-sites-build.js",
    "dev": "vite",
    "diagnose:demo": "node scripts/diagnose-demo-allocation.js",
    "normalize:yahoo": "node scripts/normalize-yahoo-players.js",
    "preview": "vite preview",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@openai/sites-vite-plugin": "^0.2.0",
    "vite": "^8.3.0"
  }
}
